# AtlasFX Web

Next.js App Router frontend for AtlasFX.

## Development

```bash
npm install
npm run dev
```

The app expects the FastAPI backend at `http://127.0.0.1:8000` unless
`NEXT_PUBLIC_ATLASFX_API_URL` is set.

## Validation

```bash
npm run lint
npm run build
```
